# This file is no longer needed and has been replaced by settings in netlify.toml
# Keeping this file for reference only

# Handle API endpoints - Forward all API requests to the API endpoints
# /api/*  /.netlify/functions/api/:splat  200

# Handle SPA routing - Support client-side routing
# /*    /index.html   200 